version: "3.9"

services:
  app:
    build: .
    container_name: mercado-central-agente
    ports:
      - "8501:8501"
    environment:
      - COHERE_API_KEY=${COHERE_API_KEY}
    volumes:
      - ./vectorstore:/app/vectorstore
      - ./logs:/app/logs
    restart: unless-stopped

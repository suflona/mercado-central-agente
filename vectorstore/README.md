Esta carpeta almacena el índice de embeddings (index.json), generado
automáticamente por src/embed_index.py cuando el agente arranca y detecta
que data/raw/ cambió (ver src/agent.py -> _load_or_build_index). No se
sube contenido real a GitHub.
